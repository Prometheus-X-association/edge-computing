---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: ${PDC}-${DEF_ZONE}-stripprefix
  namespace: ${PTX_NS}
  labels:
    app: ${PDC}
spec:
  stripPrefix:
    prefixes:
      - /${PTX_NS}/${DEF_ZONE}/${PDC}
#---
#apiVersion: traefik.io/v1alpha1
#kind: TLSOption
#metadata:
#  name: tls-opt
#  namespace: ${PTX_NS}
#spec:
#  minVersion: VersionTLS13
#---
#apiVersion: traefik.io/v1alpha1
#kind: IngressRoute
#metadata:
#  name: ${PDC}-${DEF_ZONE}
#  namespace: ${PTX_NS}
#  labels:
#    app: ${PDC}
#spec:
#  entryPoints:
#    - web
#  routes:
#    - match: PathPrefix(`/${PTX_NS}/${DEF_ZONE}/${PDC}`)
#      kind: Rule
#      middlewares:
#        - name: ${PDC}-${DEF_ZONE}-stripprefix
#          namespace: ${PTX_NS}
#      services:
#        - name: ${PDC}-${DEF_ZONE}
#          port: ${PDC_DEF_PORT}
#  tls:
#    secretName: gw-tls
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${PDC}-${DEF_ZONE}
  namespace: ${PTX_NS}
  labels:
    app: ${PDC}
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.middlewares: ${PTX_NS}-${PDC}-${DEF_ZONE}-stripprefix@kubernetescrd
    traefik.ingress.kubernetes.io/router.tls: "true"
    #traefik.ingress.kubernetes.io/router.tls.options: ${PTX_NS}-tls-opt@kubernetescrd
    #traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
    #traefik.ingress.kubernetes.io/router.tls.domains.0.main: ${GW_TLS_DOMAIN}
spec:
  ingressClassName: traefik
  rules:
    - host: ${GW_TLS_DOMAIN}
      http:
        paths:
          - path: /${PTX_NS}/${DEF_ZONE}/${PDC}
            pathType: Prefix
            backend:
              service:
                name: ${PDC}-${DEF_ZONE}
                port:
                  number: ${PDC_DEF_PORT}
  tls:
    #- secretName: gw-tls
    - hosts:
        - ${GW_TLS_DOMAIN}
---