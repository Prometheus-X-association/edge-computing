apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: pdc
  name: pdc
  namespace: ptx-edge
spec:
  selector:
    app: pdc
  type: ClusterIP
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000